const MaximumProfit = (arr, callback) => {
  //  return arr;
    callback=(arr)=>{
        console.log(arr)
    }
    return callback(arr);
};
let arr = [7, 1, 5, 3, 6, 4];
console.log(MaximumProfit(arr));
